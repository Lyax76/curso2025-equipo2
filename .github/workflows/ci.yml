name: CI
on:
  pull_request:
    branches: [ "main", "develop" ]
  push:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI OK - $(date)"
